# QuestTerminal

Android Quest utility with terminal/ADB workflows, `.blend` file selection, and a Blender-focused launcher.

```sh
./gradlew assembleDebug
```

Generated APKs are in `dist/`. The app does not bundle proprietary Quest firmware or operating systems.
